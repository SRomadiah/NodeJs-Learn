import { sum } from "../src/sum";

test("sum1(1, 2) must be 3", () => {
  const result = sum(1, 2);

  expect(result).toBe(3);
});

test("sum2(1, 2) must be 3", () => {
  const result = sum(1, 2);

  expect(result).toBe(3);
});
test("sum3(1, 2) must be 3", () => {
  const result = sum(1, 2);

  expect(result).toBe(3);
});
